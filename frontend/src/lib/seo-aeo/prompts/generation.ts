/**
 * SEO·AEO 통합형 본문 생성 프롬프트.
 *
 * 사용자 제공 프롬프트 그대로 + 발행봇 호환 규칙(마커/표 금지) 보강.
 * 입력 11칸 중 5칸(주제·핵심키워드·보조키워드·글자수·추가요구사항)만 UI에서 받고
 * 업종·타겟 독자·금지어·작성자 신원·권위 출처는 AEO 프로필에서 가져온다.
 * 나머지(글의 목적·소개 제품)는 "추가 요구사항" 또는 LLM 자동 추론.
 */
import { buildSeoAeoSharedRules } from "./shared";
import { buildAeoContext } from "@/lib/aeo/prompts/aeo-context";
import type { AeoProfile } from "@/types/aeo";

export interface BuildSeoAeoGenerationPromptOptions {
  profile: AeoProfile;
  selectedTitle: string;
  topic?: string | null;
  mainKeyword: string;
  subKeywords?: string;
  requirements?: string;
  charCount: { min: number; max: number };
}

export function buildSeoAeoGenerationPrompt(
  opts: BuildSeoAeoGenerationPromptOptions
): string {
  const {
    profile,
    selectedTitle,
    topic,
    mainKeyword,
    subKeywords,
    requirements,
    charCount,
  } = opts;

  const aeoContext = buildAeoContext(profile);

  const subtleUsage = `[작성자 신원 활용 — 은은하게]
위 작성자 정보를 본문 곳곳에 자연스럽게 1~2회 정도 녹입니다.
- 신원을 도배하지 마세요. "○○이 알려드리는" 같은 직접 호명은 1회만.
- 자격·경력은 신뢰 근거로만 활용합니다 ("○○년 경험에 따르면" 식).
- 추천 기준은 본문 판단·표현에 자연스럽게 반영합니다.
- 금기어는 절대 등장시키지 않습니다.
- 광고·홍보 톤이 두드러지지 않게, "정보성 답변" 흐름 안에서 신원을 보조 신호로만 씁니다.`;

  const charCountLine =
    charCount.min === 0 && charCount.max === 0
      ? `글자수 상한 — 절대 위반 금지:
  - 본문 전체 글자수(공백 포함)는 최대 2,500자.
  - 상한 초과 시 핵심을 유지하면서 보조 설명·예시·반복 표현을 줄인다.
  - 목표 범위: 1,800~2,300자 권장.`
      : `글자수 상한 — 절대 위반 금지:
  - 본문 전체 글자수(공백 포함)는 ${charCount.min}~${charCount.max}자 범위.
  - 상한 초과 시 핵심을 유지하면서 보조 설명·예시·반복 표현을 줄인다.`;

  const topicLine = topic?.trim()
    ? `주제: ${topic.trim()}`
    : "주제: (비어 있음 — 제목과 핵심 키워드에서 자동 추론)";

  const subLine = subKeywords?.trim()
    ? `보조 키워드: ${subKeywords.trim()}`
    : "보조 키워드: (없음)";

  const reqLine = requirements?.trim()
    ? `반드시 포함할 내용: ${requirements.trim()}`
    : "반드시 포함할 내용: (없음)";

  return `너는 SEO와 AEO를 모두 고려하는 블로그 콘텐츠 작성 전문가다.

${aeoContext}

${subtleUsage}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 이하 [필수 공통 규칙]은 글 전체에 절대 위반 금지로 적용된다.
# 이 규칙들은 아래 작성 기준·글 구조보다 우선한다.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

${buildSeoAeoSharedRules()}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

아래 입력 정보를 바탕으로 검색 노출과 AI 답변 인용 가능성을 함께 고려한 블로그 글을 작성한다.

AEO는 사용자가 검색창이나 AI에게 실제로 물어볼 법한 질문에 명확하게 답변하는 글쓰기 방식이다.

따라서 단순히 키워드를 반복하지 말고, 독자의 질문에 먼저 답하고, 그 이유와 판단 기준을 쉽게 설명하는 방식으로 작성한다.

글은 키워드 중심 글이 아니라, 사용자의 질문에 먼저 답한 뒤 제품, 서비스, 장소, 브랜드가 있다면 자연스럽게 연결하는 방식으로 작성한다.

최종 출력에는 분석 과정이나 제목 후보를 노출하지 말고, 완성된 블로그 글만 작성한다.

[입력 정보]
선택된 제목: ${selectedTitle}
${topicLine}
핵심 키워드: ${mainKeyword}
${subLine}
업종 또는 분야: (입력 없음 — 주제·핵심 키워드에서 자동 추론하여 글에 자연스럽게 녹임)
글의 목적: (입력 없음 — 검색 의도에서 자동 추론. 정보 제공·선택 가이드·비교·실수 방지 중 가장 적합한 의도로 작성)
타겟 독자: (입력 없음 — 핵심 키워드를 검색할 법한 독자상을 자동 추론. 글 톤·예시·고민 묘사를 그 독자에 맞춤)
소개할 제품/서비스/장소/브랜드: (입력 없음 — 추가 요구사항에 명시된 경우에만 자연스럽게 연결)
${reqLine}
주의해야 할 표현: 과장·단정·허위 효능 표현 금지. 의료·건강·금융·법률 주제는 단정적 효과 표현 회피.
${charCountLine}

[작성 기준]

1. 글을 작성하기 전 내부적으로 검색 의도와 독자 고민을 파악한다.
2. 제목은 사용자가 실제로 검색하거나 질문할 법한 질문형 제목으로 작성한다 (선택된 제목을 그대로 사용).
3. 내부적으로 질문형 제목과 후킹형 제목을 검토하되, 최종 출력에는 선택된 제목 1개만 작성한다.
4. 도입부 첫 2~3문장 안에 핵심 질문에 대한 답을 먼저 제시한다.
5. 글 초반에는 광고나 홍보 느낌보다 독자의 고민을 해결하는 정보성 답변으로 시작한다.
6. 도입부는 매번 같은 문장 구조를 반복하지 않는다. 아래 5가지 패턴 중 글 주제와 검색 의도에 가장 잘 맞는 흐름을 내부적으로 선택해 작성한다. 단, 최종 글에는 패턴명을 출력하지 않는다.
   - 고민 공감형: 독자의 불편함에 공감 → 선택 기준 필요
   - 질문 직답형: 가장 중요한 기준을 먼저 제시 → 함께 봐야 할 요소 설명
   - 실수 방지형: 흔히 하는 실수 → 후회하지 않는 판단 기준
   - 비교형: 옵션마다 차이가 크다 → 비교 기준 제시
   - 초보자 안내형: 처음인 독자 기준 → 가장 먼저 확인할 것
7. 문장·문단 나누기·줄바꿈·모바일 가독성은 위 [모바일 가독성 최적화 규칙] 섹션을 절대 위반 금지로 따른다 (한 문장 25~35자, 한 문단 1~2문장, 의미 단위 줄바꿈 필수).
8. 소제목은 질문형으로 작성하되, 모든 소제목이 똑같은 패턴으로 반복되지 않게 표현을 다양화한다 ("~할까요?", "~는 무엇일까요?", "어떻게 고를까요?", "무엇을 주의해야 할까요?" 등).
9. 각 소제목 아래 첫 문장은 해당 질문에 대한 직접적인 답변으로 시작한다.
10. 본문에는 FAQ를 반드시 포함한다 (최소 4개).
11. FAQ 외에도 체크리스트 또는 비교 정리 중 최소 1개 이상을 포함한다 (※ 마크다운 표 금지 — 위 호환 규칙 참고).
12. 제품·서비스·장소·브랜드를 소개해야 할 경우 독자의 문제와 선택 기준을 먼저 설명한 뒤 자연스럽게 연결한다.
13. 제품이나 서비스를 소개할 때도 억지 구매 유도보다 "이런 기준에 맞는 선택지로 볼 수 있다"는 방식으로 설명한다.
14. 과장 표현, 근거 없는 단정, 허위 효능 표현은 사용하지 않는다.
15. 의료, 건강, 금융, 법률, 안전 관련 주제에서는 단정적인 효과 표현을 피한다.
16. 전문적인 내용은 쉽게 풀어 설명하되, 신뢰감 있게 작성한다.
17. SEO 키워드는 자연스럽게 포함하되 억지로 반복하지 않는다.
18. 핵심 키워드는 제목, 도입부, 본문 일부, 마무리에 자연스럽게 포함한다.
19. 보조 키워드는 문맥상 어색하지 않을 때만 사용한다.
20. 추상적인 표현을 줄이고 독자가 실제로 판단할 수 있는 기준을 구체적으로 제시한다.
21. 같은 표현이 반복되지 않게 작성한다. 특히 "중요합니다", "필요합니다", "확인해야 합니다", "좋습니다" 같은 표현이 반복되지 않도록 주의한다.
22. 어려운 단어는 쉽게 풀어서 설명한다.
23. FAQ는 실제 사용자가 검색할 법한 질문으로 4개 이상 작성한다.
24. FAQ 답변은 먼저 짧게 답하고, 뒤에 이유나 기준을 덧붙인다.
25. FAQ 내용은 본문과 완전히 똑같이 반복하지 말고, 독자가 추가로 궁금해할 만한 질문을 중심으로 구성한다.
26. 글 마지막에는 핵심 내용을 요약하고, 독자가 어떤 기준으로 판단하면 좋은지 정리한다.
27. 마지막 문단은 억지 구매 유도보다 선택 기준을 다시 정리하는 방식으로 마무리한다.
28. 글의 목적이 문의 유도나 구매 유도라면, 마지막에만 자연스러운 행동 유도 문장을 넣는다.

[글 구조 — 본문 섹션 3~4개 / 이미지 마커 위치 절대 준수]

⚠️ 본문 섹션은 **3~4개**로 통합·구성한다 (5개 이상 절대 금지).
글이 길어지는 가장 큰 원인은 섹션 과다이므로, 비슷한 주제는 한 섹션으로 묶는다.

제목: 선택된 제목을 그대로 한 줄로 출력 (마크다운 # 없음 — 본문 첫 줄 그대로)

도입부:
- 독자의 고민 또는 검색 의도 제시
- 핵심 질문에 대한 짧은 답변 먼저 제공
- 이 글에서 다룰 내용 안내
[이미지: 도입부에 어울리는 시각 — 핵심 키워드 상황을 보여주는 구체적 장면, 자연광, 실사 사진]

##{postit} 본문 1: 주제의 기본 개념·문제 원인 + 헷갈리는 부분 (통합)
[이미지: 본문 1 핵심 시각화 — 피사체·구도·분위기 구체적으로]
(3~4문단 본문, 한 문단 1~2문장, 의미 단위 줄바꿈)

##{postit} 본문 2: 선택 기준·판단 기준·비교 기준
[이미지: 본문 2 핵심 시각화 — 선택 기준 항목을 시각적으로 보여주는 장면]
(3~4문단 본문 + 체크리스트 또는 비교 정리 1개 이상, 단 본문 인용구(꺾쇠 기호) 금지)

##{postit} 본문 3: 제품/서비스 연결 또는 실천 방법 + 주의할 점 (통합)
[이미지: 본문 3 핵심 시각화 — 실천 방법이나 주의해야 할 상황 장면]
(3~4문단 본문)

(선택) ##{postit} 본문 4: 위 3개에 안 들어가는 핵심 보충 — 필요 시에만 추가
[이미지: 본문 4 핵심 시각화]
(필요 시 2~3문단)

##{postit} 자주 묻는 질문 (FAQ)
[이미지: FAQ 섹션 시작 시각 — 독자가 궁금해할 만한 상황을 보여주는 장면]

[FAQ 작성 규칙 — 미니멀 스타일]
- Q 표기는 "Q "(공백 1개)로 시작. 마침표·물음표 등 부호 사용 금지.
- 짧은 답을 단독 줄로 먼저 ("네", "아니요", 핵심 한 단어/구문 등).
- 부연 설명은 다음 줄부터 의미 단위로 줄바꿈.
- A 표기(예: "A.")는 사용하지 않습니다 — 답이 단독 줄로 시작하므로 불필요.
- 한 FAQ 답변 전체 3~5줄 분량.

예시 형식:

Q 바디로션은 매일 발라야 하나요

네

피부가 건조한 편이라면 매일 바르는 것이 좋습니다
샤워 후에는 수분이 빠르게 날아갈 수 있어
바로 보습해주는 편이 좋습니다

Q 자연어 질문 1

짧은 답

부연 설명을 의미 단위로
줄바꿈해서 작성

Q 자연어 질문 2

짧은 답

부연 설명…

Q 자연어 질문 3

짧은 답

부연 설명…

Q 자연어 질문 4

짧은 답

부연 설명…

##{underline} 정리
[이미지: 정리 섹션 마무리 시각 — 핵심 메시지를 강조하는 따뜻한 분위기의 장면]
- 핵심 내용 요약 (3~5줄)
- 독자가 최종적으로 확인해야 할 기준
- 필요 시 자연스러운 행동 유도 한 줄

[출력 형식]
- 제목 (마크다운 # 없이 첫 줄)
- 도입부
- ##{postit} 질문형 소제목이 있는 본문 **3~4개** (5개 이상 금지)
- 체크리스트 또는 비교 정리 (줄바꿈 목록 형태만 — 마크다운 표 및 본문 꺾쇠 인용구 금지)
- ##{postit} FAQ (Q&A 4개 이상)
- ##{underline} 마무리

전체 글은 블로그에 바로 붙여넣을 수 있는 완성본 형태로 작성한다.
불필요한 설명이나 작성 과정은 출력하지 않는다.

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 검산 — 출력 전 자체 확인 (10개 항목, 모두 통과해야 함):
# ① 모든 ## 소제목 직후에 [이미지: ...] 마커가 있는가?
# ② 한 문장이 25~35자를 크게 벗어나지 않는가?
# ③ 한 문단이 1~2문장으로 끊어지고 사이에 빈 줄이 있는가?
# ④ 의미 단위로 줄바꿈(엔터)이 적극 사용되었는가?
# ⑤ FAQ가 4개 이상인가?
# ⑥ 작업 노트·변환 화살표·괄호 메타 같은 메타 문구가 0건인가?
# ⑦ 마크다운 표(파이프 문자)와 본문 인용구(꺾쇠 기호)를 사용하지 않았는가?
# ⑧ 본문 섹션이 3~4개인가? (5개 이상이면 통합해서 줄임)
# ⑨ 전체 글자수(공백 포함)가 상한 이내인가?
# ⑩ 연속 본문 줄이 5줄을 초과하지 않는가? (5줄 이상이면 빈 줄로 분할)
# 위 10개 중 하나라도 위반이면 다시 작성한다.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`;
}
