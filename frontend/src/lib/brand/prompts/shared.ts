/**
 * 브랜드 글쓰기 공통 규칙 — SEO / 가독성 / 금기 / 응답 형식.
 *
 * 모든 4개 템플릿(소개/정보/가치입증/상세) 프롬프트에 공통으로 합성됨.
 */

export const BRAND_RESPONSE_FORMAT = `[응답 형식 — 절대 위반 금지]
1. 출력은 **마크다운**으로 작성합니다.
2. 글 맨 위에 \`<HOOK>\` 블록을 둡니다 — 썸네일/히어로 컷용 1~2 문장.
   예: <HOOK>크루즈 패키지 여행 가격 비교 공짜인 줄 알았던 기항지의 배신</HOOK>
3. 본문은 짧은 문단으로 끊어 작성합니다 (2~4줄마다 분할).
4. 소제목은 \`## \`로 시작하며, 반드시 스타일 마커를 함께 명시합니다 (예: \`##{postit} 소제목\`). 자세한 규칙은 [소제목 인용구 스타일] 섹션 참고.
5. 이미지가 들어갈 자리에는 \`[이미지: 묘사]\` 마커를 넣습니다.
   예: [이미지: 크루즈 갑판에서 바다를 바라보는 노부부의 따뜻한 뒷모습]
6. 본문 안에 광고 직접 표현("꼭 사세요", "지금 결제하세요" 등) 금지.

[출력 메타 금지 — 절대 위반 금지]
다음 항목은 본문에 절대 출력하지 마라. 본문 = 최종 글 그 자체이며, 어떤 부연·노트·설명도 붙이지 않는다.

· 사고 과정·작업 노트
  - 예: "(이 부분은 인테리어 문맥인데, 크루즈 문맥으로 바꿔야 합니다)"
  - 예: "원본은 X였지만 Y로 수정했습니다"
  - 예: "(이 단락은 견본 글의 자기 고백 부분을 따라 작성)"

· 변환 화살표·번역 노트
  - 예: "→ ①", "→ ②", "변경:", "수정:", "원본 → 변환", "Before → After"
  - 본문 흐름 안에서 자연스러운 인과 표현은 OK ("그래서", "그 결과" 등). 단순 화살표·라벨 형태만 금지.

· 원본 + 변환 동시 출력
  - 예: "① 저렴한 자재... → ① 현지 추가 비용..." (한 번에 두 버전 적기 금지)
  - 예: "원본: ... / 변환: ..."
  - 새 도메인 어휘로 처음부터 한 번만 작성한다.

· 괄호 안 메타 설명
  - 예: "(이건 견본 따라 쓴 부분)"
  - 예: "(주: 본 글은 톤만 차용)"
  - 본문 흐름 안의 자연스러운 부연 괄호는 OK (예: "(군산 기준 2시간 이상)"). 작가의 작업 메모만 금지.

· 견본 글에 등장하는 다른 산업·지역·고유명사
  - 예: 크루즈 글인데 "인테리어", "AS", "공사", "리모델링", "자재", "군산", "전주", "익산", "서천", "시크릿 인테리어" 등이 본문에 등장 금지.
  - 이런 어휘는 견본 학습용일 뿐, 새 글 본문에는 메인 키워드 도메인의 어휘로 처음부터 새로 작성한다.

검산: 본문에 위 5가지가 모두 0건인가? 1건이라도 있으면 다시 작성한다.`;

export const BRAND_SEO_RULES = `[SEO 규칙]
- 메인 키워드는 글 전체에서 5~7회 자연스럽게 반복
- 메인 키워드는 첫 문단 안에 반드시 1회 등장
- 보조 키워드도 본문에 자연스럽게 녹여냄 (강제 X)`;

export const BRAND_READABILITY_RULES_DEFAULT = `[가독성 규칙 — 절대 위반 금지]
- 한 문장 목표: 20~30자 (짧고 임팩트 있게, 호흡 짧게)
- 한 문장이 60자 넘어가는데 마침표 없이 이어지는 것 절대 금지
- 같은 주제·감정·시점이면 한 묶음 (3~4줄, 줄바꿈만 하고 빈 줄 없음)
- 다른 주제·감정으로 넘어갈 때만 빈 줄 1개 — 기계적으로 매 2줄마다 빈 줄 넣지 말 것
- 문단 사이 적절한 여백 (의미 단위 전환에서만)
- 긴 문단·긴 문장 절대 금지
- 스크롤하면서 끊기지 않게 리듬감 있게 구성
- 모바일 기준 가독성 우선`;

export const BRAND_READABILITY_RULES_FOLLOW_REFERENCE = `[가독성 규칙 — 견본 호흡 우선]
- 견본의 문장 길이·문단 호흡을 그대로 따라간다 (견본 모드의 본질)
- 단 지나치게 만연체(한 문장 100자+ 마침표 없이 이어짐)만 피한다 — 그 외엔 견본 톤 보존
- 문단 사이 적절한 여백 (의미 단위 전환에서만)
- 스크롤하면서 끊기지 않게 리듬감 있게 구성
- 모바일 기준 가독성 우선`;

export const BRAND_AVOID_AD_TONE = `[톤·금기 규칙]
- 광고 느낌이 절대 들지 않게 작성
- 신뢰 중심
- 도발적이되 정직 (예: "솔직히 말씀드리면", "업계에서 욕 먹습니다")
- 자랑 일변도 X — 권위는 수치·구체 사례·고객 멘트로 입증
- 똑같은 표현 반복 X — 키워드 반복 시 같은 내용은 다른 표현으로`;

export const BRAND_IMAGE_PLACEMENT_RULES = `[이미지 배치 — 절대 위반 금지]

본문 곳곳에 \`[이미지: 구체 묘사]\` 마커를 박아 텍스트 큰 덩어리를 시각적으로 끊는다. 아래 단계를 순서대로 수행하라.

1단계 — 소제목 커버리지 (예외 없음)
- 모든 ##{스타일} 소제목 직후 빈 줄 하나 다음에 [이미지: 구체 묘사] 마커 1개 필수
- 자연스러움보다 우선 — 빠지면 재작성

2단계 — 도입부 커버리지 (긴 도입부 안전망)
- HOOK 블록과 첫 소제목 사이 도입부가 4단락 이상이면, 그 중간에 [이미지: 묘사] 마커 1개 필수
- 자연스러운 위치: 자기 고백·사명감·반전 같은 감정 전환 단락 직후
- 짧은 도입부(3단락 이하)면 생략 가능

3단계 — 잔여 분산 (총 5~7개 목표)
- 1+2단계 후 부족하면 아래에 추가:
  · 500자 이상 이어지는 긴 본문 구간 중간
  · 전후 비교·반전 지점
  · 감정 피크 지점 (독자가 멈출 만한 순간)

4단계 — 마커 작성 규칙
- 피사체·구도·조명·감정까지 구체적으로 묘사한다.
- 좋은 예: [이미지: 비싼 결제 영수증을 들고 한숨 쉬는 30대 여성, 어둑한 카페, 근심 어린 표정]
- 나쁜 예: [이미지: 비용], [이미지: 분위기], [이미지: 제품 사진]
- 마커 줄 앞뒤에 빈 줄 1줄씩 확보
- 마커는 본문 안에만 — 해시태그·제목 안에는 금지

검산 체크리스트 (글 완성 전 반드시 확인)
1. 모든 ##{스타일} 소제목 직후에 마커가 있는가?
2. 도입부가 4단락 이상이면 중간에 마커가 있는가?
3. 총 마커 수가 5~7개인가?
4. 마커 묘사가 추상적이지 않고 구체적인가?`;

export const BRAND_HEADING_QUOTE_RULES = `[소제목 인용구 스타일 — 필수]

발행 봇이 소제목을 네이버 인용구 박스로 정확히 변환하려면 마크다운 헤더에 스타일 마커를 반드시 명시해야 합니다. 마커가 없으면 인용구 박스가 비어 보이는 사고가 납니다.

1단계: 메인 스타일 1개 선택 (대부분의 소제목에 동일 적용)
글 전체 톤에 어울리는 스타일 1개를 골라, 거의 모든 소제목에 같은 스타일로 사용한다.

| 글 톤 | 메인 스타일 |
|---|---|
| 일상·감성·스토리 | line (세로선) |
| 정보·팁·후기·폭로 | postit (포스트잇) |
| 격식·전문가·정의 | underline (밑줄) 또는 corner (꺾쇠) |
| 친근·대화체 | bubble (말풍선) |
| 명언·격언 분위기 | default (큰따옴표) |

2단계: 예외 — 성격이 분명히 다른 소제목 1~2개만 다른 스타일
다음 경우에만 메인 스타일과 다른 스타일을 사용한다. 그 외엔 무조건 메인 스타일로 통일.
- 글 마지막의 결론·마무리·핵심 정리 소제목 → 메인이 line이면 postit, 메인이 postit이면 underline 등
- 본문에 인용·명언이 핵심인 소제목 → default

형식 (반드시 준수)
- 거의 모든 소제목: ##{메인스타일} 소제목
- 예외 1~2개: ##{다른스타일} 소제목
- 스타일을 비워두면 (## 소제목) 발행 시 인용구가 비어 표시되므로 절대 비우지 마라.

결과 예시 (정보·폭로 톤, 소제목 5개)
- ##{postit} 겉보기엔 싸지만 눈덩이처럼 불어나는 함정
- ##{postit} 현장에서 마주하는 추가 지불의 벽
- ##{postit} 우리가 정직한 올인클루시브를 고집하는 이유
- ##{postit} 가짜 인증샷이 아닌 진짜 도시를 만나는 법
- ##{underline} 가장 싼 여행이 아닌 제대로 된 여행을 선택하세요   ← 마무리만 예외`;

export const BRAND_ASSET_USAGE_RULES = `[자산 사용 규칙 — 필수]
- 브랜드 컨텍스트의 에피소드·시그니처 표현·비유는 글 맥락에 맞을 때만 자연스럽게 활용한다. 모든 자산을 강제로 다 박지 마라.
- 같은 자산을 다른 글에서도 쓸 수 있되, 이전 글과 동일한 문장·표현은 절대 금지. 같은 사실(예: "14년 경력")이라도 매번 다른 각도·다른 비유·다른 문장 구조로 풀어낸다.
- 윤희와 임두환을 무리하게 함께 등장시키지 마라. 글 주제에 맞춰 한 명만 등장하거나, 한 명을 주·한 명을 보조로 자연스럽게 배치한다.
- 감동 에피소드(예: "50년 만에 손잡은 노부부")는 신뢰·여유·휴식 문맥에서만 활용한다. 가격 폭로·비교 글에 억지로 끼워 넣지 마라.`;

/** 사용자 입력 주제가 있을 때만 합성. 없으면 빈 문자열 반환. */
export function buildTopicSection(topic?: string | null): string {
  if (!topic || topic.trim().length === 0) return "";
  return `[글의 주제 — 사용자 지정]
이 글의 주제는 다음과 같습니다:
${topic.trim()}

본문은 위 주제를 정확히 다루어야 합니다. 키워드는 도구일 뿐, 글의 중심은 이 주제입니다.`;
}

/**
 * 가독성 규칙 모드.
 * - "default": 일반 변형 — 짧은 문장 강제 (20~30자 목표, 60자 금지)
 * - "follow-reference": 견본 모드(info-custom, info-structure-based) — 견본 호흡 보존
 */
export type SharedRulesMode = "default" | "follow-reference";

/** 모든 공통 규칙을 한 번에 합쳐 반환. 템플릿 프롬프트 끝부분에 붙임. */
export function buildSharedRules(options?: { mode?: SharedRulesMode }): string {
  const mode = options?.mode ?? "default";
  const readabilityRules =
    mode === "follow-reference"
      ? BRAND_READABILITY_RULES_FOLLOW_REFERENCE
      : BRAND_READABILITY_RULES_DEFAULT;
  return [
    BRAND_RESPONSE_FORMAT,
    BRAND_SEO_RULES,
    readabilityRules,
    BRAND_AVOID_AD_TONE,
    BRAND_HEADING_QUOTE_RULES,
    BRAND_IMAGE_PLACEMENT_RULES,
    BRAND_ASSET_USAGE_RULES,
  ].join("\n\n");
}

// ─────────────────────────────────────────────
// 제목 전용 가드레일 — 제목 빌더(title.ts)에서만 import.
// buildSharedRules() 에는 포함하지 않음 (본문 프롬프트 누수 방지).
// ─────────────────────────────────────────────

/** 모든 제목 후보가 반드시 지켜야 할 기본 규칙. 4개 템플릿 공통. */
export const BRAND_TITLE_BASE_RULES = `[제목 기본 규칙 — 절대 위반 금지]
- 메인 키워드는 제목 맨 앞에 고정한다. 중간이나 끝에 두지 마라.
- 문장부호 일체 금지: 마침표·쉼표·따옴표·괄호·느낌표·물음표·특수문자 모두 금지.
- 띄어쓰기와 단어만으로 의미 전달. 자연스러운 문장형으로.
- 추상어·광고어 금지 — 다음 단어와 그 변형은 절대 사용 금지:
  · 최고의 / 최상의 / 최고급 / 탑클래스
  · 프리미엄 / 럭셔리
  · 혁신적인 / 혁신
  · 완벽한 / 완벽
  · 특별한 / 특별
- 현실 표현을 우선 사용 (광고 톤이 아닌 일상 톤):
  · "실제로 많이 생기는"
  · "생각보다 자주 놓치는"
  · "가장 많이 후회하는"
  · "직접 겪어보니"
  · "현장에서 자주 보는"`;

/**
 * 정보성글 전용 — 회사명·인물명·서비스명 0건 강제.
 *
 * 정보성글은 "익명 업계 전문가" 톤으로 작성되어야 하며, 제목에 브랜드 단서가
 * 노출되면 전략 자체가 깨진다. info 템플릿 제목 빌더에서만 부착.
 */
export const BRAND_TITLE_ZERO_EXPOSURE_RULES = `[정보성글 제목 — 브랜드 노출 금기 — 절대 위반 금지]
다음이 제목에 등장하면 절대 안 됨:
1. 회사명·브랜드명 (자사 라벨 포함)
2. 대표·임원·직원 실명 (가명·이니셜도 X)
3. 자사 서비스·상품 고유명사
4. 시그니처 표현 그대로 인용
5. "저희", "당사", "우리 [회사]" 같은 자사 지칭 표현
6. 브랜드를 추측 가능하게 만드는 직간접 단서

정보성글 제목은 익명의 업계 종사자가 쓴 정보 글처럼 보여야 한다.`;
