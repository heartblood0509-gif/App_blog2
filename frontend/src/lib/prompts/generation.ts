import type { NarrativeType, ToneType, SelectedProduct } from "@/types";
import { getTonePrompt } from "./tone-rules";
import { buildProductContext } from "./product-placement";
import { getDefaultReference } from "./default-reference";
import { getNarrativePrompt } from "./narrative-templates";

interface GenerationParams {
  products: SelectedProduct[];
  /** null이면 custom 레퍼런스 모드(서사 템플릿 미적용, 레퍼런스 우선) */
  narrativeType: NarrativeType | null;
  toneType: ToneType;
  toneExample?: string;
  mainKeyword: string;
  subKeywords?: string;
  persona?: string;
  requirements?: string;
  charCount: { min: number; max: number };
  selectedTitle: string;
  referenceAnalysis?: string;
}

export function buildGenerationPrompt(params: GenerationParams): string {
  const {
    products,
    narrativeType,
    toneType,
    toneExample,
    mainKeyword,
    subKeywords,
    persona,
    requirements,
    selectedTitle,
    referenceAnalysis,
  } = params;

  const sections: string[] = [];

  // ──────────────────────────────────────
  // 섹션 -1: 🚨 최우선 규칙 (narrativeType이 지정된 경우만)
  //  - 본문 첫 문단을 narrativeType에 맞게 강제하는 가장 강력한 규칙
  //  - 도입부 형식 강제 + 금지 패턴 명시(나쁜 예) + AI 자가 검증 체크리스트
  //  - 프롬프트 전체에서 맨 먼저 등장 → AI 주의도 최상위
  // ──────────────────────────────────────
  if (narrativeType === "conclusion-first") {
    sections.push(`# 🚨 최우선 규칙 — 반드시 먼저 읽고 지킬 것

이 글은 **결론 선공형** 구조입니다. 아래 규칙이 다른 모든 지시(레퍼런스·제품 후기·서사 템플릿)보다 **우선**합니다.

## 본문 첫 문장·첫 문단 강제 규칙

1. HOOK 블록 다음 **본문 첫 문장은 반드시** "지금은 ~", "이제는 ~", "요즘은 ~" 중 하나로 시작하세요
2. **첫 문단 전체가 "현재 해결/개선된 상태"** 를 보여줘야 합니다
   - 예: "~ 거의 안 신경 쓰임", "~ 걱정 없이 지냄", "~ 확실히 달라짐"
3. 문제·스트레스·고민 얘기는 **두 번째 문단부터** "근데 예전에는 ~", "돌이켜보면 그때 ~" 같은 **과거 회상** 형태로 도입합니다

## ⛔ 절대 금지 패턴 (이렇게 쓰면 실패 = 다시 쓰기)

아래 패턴은 **감정 선공형** 구조입니다. 결론 선공형에서는 절대 쓰지 마세요:

- ❌ "요즘 ~ 스트레스였음" (형식어는 "요즘"이지만 내용이 현재 문제)
- ❌ "요즘 ~ 한숨 쉬는 게 습관" (현재 고민이 지속 중인 표현)
- ❌ "~ 때문에 힘들었음" / "~ 때문에 고민이었음"
- ❌ "어느 날부터 ~ 문제 생겼음" / "갑자기 ~ 시작했음"
- ❌ 첫 문단이 증상·불편함·걱정 묘사로만 가득한 경우

## 🔍 자가 검증 (본문 쓰기 시작하기 전, 속으로만 확인. 본문에 출력하지 말 것)

본문 첫 문단을 쓰기 전에 아래를 스스로 체크하세요:

- [ ] 내 첫 문장이 "**현재 해결/개선된 상태**"를 말하고 있는가?
- [ ] 문제·스트레스 얘기는 **두 번째 문단 이후**로 미뤘는가?

둘 중 하나라도 NO면 **처음부터 다시 쓰세요**. 이 검증 내용은 본문에 절대 포함하지 마세요.`);
  } else if (narrativeType === "empathy-first") {
    sections.push(`# 🚨 최우선 규칙 — 반드시 먼저 읽고 지킬 것

이 글은 **감정 선공형** 구조입니다. 아래 규칙이 다른 모든 지시(레퍼런스·제품 후기·서사 템플릿)보다 **우선**합니다.

## 본문 첫 문장·첫 문단 강제 규칙

1. HOOK 블록 다음 **본문 첫 문장은** "요즘 ~", "갑자기 ~", "어느 날부터 ~" 류 **현재 진행형 고민 제기**로 시작하세요
2. **첫 문단 전체가 "현재 진행 중인 문제·스트레스 상황"** 을 구체적으로 묘사해 독자 공감을 유도합니다
3. 해결·변화 얘기는 글 **중반 이후**에 배치합니다

## ⛔ 절대 금지 패턴 (이렇게 쓰면 실패 = 다시 쓰기)

아래 패턴은 **결론 선공형** 구조입니다. 감정 선공형에서는 절대 쓰지 마세요:

- ❌ "지금은 ~ 괜찮아짐"
- ❌ "이제는 ~ 해결됨" / "이제는 ~ 걱정 없음"
- ❌ "바꾸고 나서 ~ 달라짐"
- ❌ 첫 문단이 해결된 현재 상태 묘사로 시작하는 경우

## 🔍 자가 검증 (본문 쓰기 시작하기 전, 속으로만 확인. 본문에 출력하지 말 것)

본문 첫 문단을 쓰기 전에 아래를 스스로 체크하세요:

- [ ] 내 첫 문장이 "**현재 진행형 고민·문제**"를 말하고 있는가?
- [ ] 해결·변화 얘기는 글 **중반 이후**로 배치했는가?

둘 중 하나라도 NO면 **처음부터 다시 쓰세요**. 이 검증 내용은 본문에 절대 포함하지 마세요.`);
  }

  // ──────────────────────────────────────
  // 섹션 0: 응답 형식 (HOOK 블록 필수)
  // ──────────────────────────────────────
  sections.push(`## 응답 형식 (반드시 이 구조로 시작)

글을 쓰기 전에, 맨 처음에 아래 <HOOK> 블록을 먼저 출력해야 합니다.

<HOOK>
(여기에 "후킹 이미지" 묘사 한 줄을 씁니다)
</HOOK>

(그 다음부터 평소대로 자연스러운 본문 마크다운을 시작)

### 후킹 이미지란?
- 이 글을 네이버 블로그에 발행했을 때, **독자가 본문을 읽기 전에 맨 위에서 먼저 보게 되는 1장의 대표 이미지**입니다
- 글의 첫 인상을 좌우하는 썸네일/히어로 컷 역할 (독자가 "이 글 읽어볼까" 결정하는 순간)
- 제목("${selectedTitle}")과 메인 키워드("${mainKeyword}"), 그리고 본문 전체의 감정·상황을 상징하는 1장

### 후킹 이미지 묘사 작성 규칙
1. 피사체 + 구도 + 조명 + 감정까지 구체적으로 한 줄에 묘사
2. 대괄호([이미지:]) 없이 **순수 묘사 텍스트만**
3. 본문 맥락에서 자연스럽게 떠오르는 장면 (추상 금지)
4. 한국인 피사체, 실사 DSLR 느낌
${
  narrativeType === "conclusion-first"
    ? `
### HOOK 방향 (이 글의 서사 구조에 맞추세요 — 중요)
이 글은 **결론 선공형**입니다. HOOK은 본문 도입부의 프레이밍을 결정하므로 반드시 아래 방향을 지키세요:
- **현재 해결/개선된 평온한 상태**를 담은 장면으로 묘사
- 긍정적 감정 키워드 사용: 만족, 평온, 여유, 편안함, 미소, 자신감
- 부정적 감정 키워드 금지: 망연자실, 좌절, 초조, 근심, 걱정, 한숨
- 과거 문제 상황은 HOOK이 아니라 본문 후반부에서 회상합니다

### 좋은 예 (결론 선공형)
<HOOK>
거울 앞에서 차분히 머리를 정돈하며 만족스럽게 미소 짓는 20대 여성, 아침 자연광, 평온하고 여유로운 표정
</HOOK>`
    : narrativeType === "empathy-first"
    ? `
### HOOK 방향 (이 글의 서사 구조에 맞추세요 — 중요)
이 글은 **감정 선공형**입니다. HOOK은 본문 도입부의 프레이밍을 결정하므로 반드시 아래 방향을 지키세요:
- **현재 진행 중인 문제·고민 상황**을 담은 장면으로 묘사
- 부정적 감정 키워드 사용: 좌절, 초조, 망연자실, 근심, 걱정, 한숨

### 좋은 예 (감정 선공형)
<HOOK>
욕실 거울 앞에서 빠진 머리카락 뭉치를 손에 쥐고 망연자실한 20대 여성, 아침 자연광, 좌절감 어린 표정
</HOOK>`
    : `
### 좋은 예
<HOOK>
욕실 거울 앞에서 빠진 머리카락 뭉치를 손에 쥐고 망연자실한 20대 여성, 아침 자연광, 좌절감 어린 표정
</HOOK>`
}

### 나쁜 예
<HOOK>
[이미지: 탈모 사진]
</HOOK>
(대괄호 금지, 추상적 금지)

<HOOK>
탈모로 스트레스 받는 느낌
</HOOK>
(피사체·구도 없음)

### 중요
- <HOOK></HOOK> 블록은 글 전체에서 **맨 처음에 딱 1번만** 출현합니다
- 이 블록 다음에는 기존 본문 규칙(소제목, 잔여 이미지 마커, 해시태그)을 그대로 따릅니다
- 본문 안에는 <HOOK> 블록 넣지 마세요`);

  // ──────────────────────────────────────
  // 섹션 1: 역할 + 필수 규칙 4개
  // ──────────────────────────────────────
  sections.push(`# 역할
너는 실제 경험을 자연스럽게 풀어쓰는 후기성 블로그 작가야
아래 레퍼런스 글의 톤과 흐름을 따라서 새로운 글을 써

## 반드시 지킬 것 (이 4가지만 확실히 지켜)

1. 아래 레퍼런스 글의 **톤·말투·문장 리듬·자연스러움만** 따라 써
   구조와 흐름은 아래 "서사 구조" 섹션을 **최우선**으로 따를 것
   규칙을 의식하지 말고 경험을 풀어쓰듯 자연스럽게

2. 메인 키워드 "${mainKeyword}"를 글 전체에 5회 이상 자연스럽게 녹여

3. 광고 느낌 절대 금지
   읽는 사람이 "이거 광고 아닌가" 라고 느끼면 실패

4. 문장부호 사용 금지
   마침표 쉼표 느낌표 물음표 따옴표 전부 사용하지 말 것`);

  // ──────────────────────────────────────
  // 섹션 2: 서사 구조 (narrativeType이 지정된 경우만 — 최우선 가이드)
  //  - 글의 뼈대(도입부 → 전개 → 마무리)를 이 템플릿대로 구성
  //  - 레퍼런스(섹션 2.5)와 구조가 충돌하면 이 섹션이 우선
  //  - 레퍼런스보다 앞에 배치 = AI가 구조를 먼저 결정하고 레퍼런스로 톤만 입히도록
  // ──────────────────────────────────────
  if (narrativeType) {
    sections.push(`${getNarrativePrompt(narrativeType)}

### 중요 (이 구조가 레퍼런스보다 우선)
- 위 흐름대로 글의 **뼈대**를 구성하세요. 특히 **도입부는 1단계 내용을 반드시 먼저** 배치하세요
- 모든 단계를 같은 비중으로 쓸 필요는 없습니다. 자연스럽게 흐름을 이어가되 순서는 지키세요
- 아래 레퍼런스의 구조가 이와 달라도 **이 서사 구조를 우선**합니다 — 레퍼런스는 말투·리듬만 참고`);
  }

  // ──────────────────────────────────────
  // 섹션 2.5: 레퍼런스 글
  //  - narrativeType이 지정되면: "톤·말투·리듬"만 참고, "구조/흐름"은 위 서사 템플릿을 따름
  //  - narrativeType이 null이면: 레퍼런스 글의 톤·구조를 모두 따름 (custom 모드)
  // ──────────────────────────────────────
  const referenceText =
    referenceAnalysis ?? getDefaultReference(narrativeType);

  if (referenceText) {
    if (narrativeType) {
      sections.push(`## 레퍼런스 (톤·말투·문장 리듬만 참고)
- 이 글의 **문장 느낌, 말투, 줄바꿈 리듬, 자연스러움**을 배우세요
- **내용과 구조는 복사하지 마세요**. 구조는 위 "서사 구조" 섹션을 따릅니다
- 레퍼런스의 서사 구조가 선택된 서사 구조와 다를 수 있지만, 그 경우 **위 서사 구조를 우선**합니다

${referenceText}`);
    } else {
      sections.push(`## 레퍼런스 (이 글의 톤과 흐름을 따라 써)
내용을 복사하지 말고 느낌만 참고해

${referenceText}`);
    }
  }

  // ──────────────────────────────────────
  // 섹션 3: 제품 정보 + 실제 후기
  // ──────────────────────────────────────
  const productContext = buildProductContext(products);
  if (productContext) {
    sections.push(productContext);
  }

  // ──────────────────────────────────────
  // 섹션 4: 사용자 설정
  // ──────────────────────────────────────
  const userInput: string[] = [`## 이 글의 조건`];
  userInput.push(`- 제목: ${selectedTitle}`);
  userInput.push(`- 메인 키워드: ${mainKeyword}`);
  if (subKeywords) userInput.push(`- 서브 키워드: ${subKeywords}`);
  if (persona) userInput.push(`- 글쓴이: ${persona}`);
  if (requirements) userInput.push(`- 추가 요청: ${requirements}`);

  // 말투
  const toneLabel = toneType === "존댓말" ? "존댓말 (친한 언니/형 느낌)" :
    toneType === "반말" ? "반말 (동갑 친구 느낌)" : "음슴체 (커뮤니티 후기 느낌)";
  userInput.push(`- 말투: ${toneLabel}`);

  if (toneExample) {
    userInput.push(`- 말투 예시 (이 톤을 따라):\n${toneExample}`);
  }

  sections.push(userInput.join("\n"));

  // ──────────────────────────────────────
  // 섹션 5: 참고 사항 (후순위)
  // ──────────────────────────────────────
  sections.push(`## 반드시 지킬 것 (본문 구조 필수 규칙)
- 소제목은 ## 형식으로 4~5개 배치하되 다양한 스타일로 (질문형 상황묘사형 감정형 반전형 등)
- 글 마지막에 해시태그 8개
- **한 줄에 한 문장 원칙** (짧게 끊어서 자주 줄바꿈)
- **문단은 2~4줄을 유지. 5줄 이상 연속된 긴 문단 절대 금지**
- **중요한 문장은 단독 줄로 분리하여 강조** (감정 변화의 핵심 포인트를 한 줄로)
- **문단 사이에는 빈 줄로 충분한 여백 확보** (모바일 가독성 필수)
- 독자가 스크롤을 끊지 않고 리듬감 있게 읽어 내려갈 수 있도록 흐름 설계
- 제품은 글의 60~70% 지점에서 자연스럽게 등장
- "키워드"라는 단어 자체를 사용하지 말 것
- 네이버 금칙어 피하기: 치료 완치 처방 약효 부작용 무료 공짜
- 글의 시작은 제목 없이 본문부터 바로
- ⛔ 본문을 > 로 감싸지 마세요. 인용구는 소제목(##) 전용입니다

## 소제목 인용구 스타일 선택 규칙

한 글 안의 인용구 박스는 **기본적으로 같은 스타일로 통일**해야 자연스럽습니다. 모든 소제목마다 다른 스타일을 쓰면 산만합니다.

### 1단계: 메인 스타일 1개 선택 (대부분의 소제목에 동일 적용)
글 전체 톤에 어울리는 스타일 1개를 골라, 거의 모든 소제목에 같은 스타일로 사용하세요.

| 글 톤 | 메인 스타일 |
|---|---|
| 일상/감성 스토리 | line (세로선) |
| 정보·팁·후기 위주 | postit (포스트잇) |
| 격식·전문가 톤 | underline (밑줄) 또는 corner (꺾쇠) |
| 친근·대화체 | bubble (말풍선) |
| 명언·격언 분위기 | default (큰따옴표) |

### 2단계: 예외 — 성격이 분명히 다른 소제목 1~2개만 다른 스타일
다음 경우에만 메인 스타일과 다른 스타일을 사용하세요. 그 외에는 무조건 메인 스타일로 통일.
- 글 마지막의 결론·마무리·핵심 정리 소제목 → 메인이 line이면 postit, 메인이 postit이면 underline 등
- 본문에 인용·명언이 핵심인 소제목 → default

### 형식
- 거의 모든 소제목: \`##\{메인스타일\} 소제목\`
- 예외 1~2개: \`##\{다른스타일\} 소제목\`
- 스타일 명시 안 하면 (\`## 소제목\`) → 자동 배정됨 (모두 같은 스타일로 통일)

### 결과 예시 (글 톤이 "정보·팁"인 경우, 소제목 5개)
- ##\{postit\} 좋다는 건 다 써봤음
- ##\{postit\} 원인을 들여다보기 시작함
- ##\{postit\} 진짜 효과를 본 순간
- ##\{postit\} 사용 후 달라진 변화
- ##\{underline\} 마지막으로 정리하면     ← 마무리 소제목만 예외

## 이미지 배치 (반드시 지켜 — 2단계 순서대로)
본문에 \`[이미지: 구체적 시각 묘사]\` 형식의 마커를 삽입합니다. 아래 2단계를 순서대로 수행하세요.

### 1단계: 소제목 커버리지 (절대 누락 금지)
- 글에 소제목(## 로 시작하는 줄)이 N개 있으면, **각 소제목 바로 다음 줄에 마커 1개씩 총 N개**를 반드시 배치합니다
- 형식 예:
  \`\`\`
  ##\{line\} 소제목

  [이미지: 구체적 시각 묘사]

  본문 문장들...
  \`\`\`
- 이 규칙은 **예외 없음**. 자연스러움보다 우선합니다
- 본문을 다 쓴 후 소제목 개수를 세고, 각 소제목 바로 아래에 마커가 있는지 반드시 검산하세요

### 2단계: 잔여 이미지 분산 (총 8~10장 목표)
- 1단계로 N개를 배치한 후, 남은 (8 - N) ~ (10 - N) 개를 아래 위치에 배치합니다:
  - **500자 이상** 이어지는 긴 본문 중간
  - **전/후 비교** 구간 (연속 2개 마커, 사이에 빈 줄만)
  - **감정 피크/반전** 지점 (독자가 멈출 만한 순간)

### 마커 작성 규칙
- 피사체·구도·조명·감정까지 구체적으로 묘사
  - 좋은 예: \`[이미지: 욕실 거울 앞에서 젖은 머리를 수건으로 털어내는 30대 여성, 자연광, 근심 어린 표정]\`
  - 나쁜 예: \`[이미지: 제품 사진]\`, \`[이미지: 깨끗한 느낌]\` (추상적 금지)
- 마커 줄 앞뒤에 반드시 **빈 줄 1줄씩** 확보
- 마커는 **본문 안에만**. 해시태그·제목 안에는 넣지 말 것

### 검산 체크리스트 (글 완성 전에 확인)
1. 소제목(##) N개 → 각 바로 아래에 마커 있는가? (없으면 추가)
2. 총 마커 수가 8~10개인가?
3. 대비 구간이 아닌 마커는 단독(앞뒤에 본문 최소 1문장)인가?
4. 본문에 > 로 시작하는 줄이 없는가? (있으면 > 제거)`);

  return sections.join("\n\n");
}
